# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActions (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --generate-configuration GitHubActions_release --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: 'Cache: .nuke/temp, ~/.nuget/packages'
        uses: actions/cache@v4
        with:
          path: |
            .nuke/temp
            ~/.nuget/packages
          key: ${{ runner.os }}-${{ hashFiles('**/global.json', '**/*.csproj', '**/Directory.Packages.props') }}
      - name: 'Run: Release'
        run: ./build.cmd Release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: 'Publish: SensorPal-client-android.zip'
        uses: actions/upload-artifact@v5
        with:
          name: SensorPal-client-android.zip
          path: artifacts/SensorPal-client-android.zip
      - name: 'Publish: SensorPal-server-win-x64.zip'
        uses: actions/upload-artifact@v5
        with:
          name: SensorPal-server-win-x64.zip
          path: artifacts/SensorPal-server-win-x64.zip
      - name: 'Publish: SensorPal-client-win-x64.zip'
        uses: actions/upload-artifact@v5
        with:
          name: SensorPal-client-win-x64.zip
          path: artifacts/SensorPal-client-win-x64.zip
