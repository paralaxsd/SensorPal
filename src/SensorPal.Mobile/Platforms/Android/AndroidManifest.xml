<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<application android:allowBackup="true" android:icon="@mipmap/appicon"
               android:supportsRtl="true" android:label="Sensor Pal™"
               android:usesCleartextTraffic="true">
		<!-- Foreground service for background noise-event notifications.
		     The name must match the [Register] attribute on SensorPalForegroundService.
		     foregroundServiceType="dataSync" is required on API 34+. -->
		<service android:name="org.speckdrumm.sensorpal.mobile.SensorPalForegroundService"
		         android:foregroundServiceType="dataSync"
		         android:exported="false" />
	</application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
	<uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
	<!-- POST_NOTIFICATIONS is a runtime permission on Android 13+ (API 33). -->
	<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
</manifest>
