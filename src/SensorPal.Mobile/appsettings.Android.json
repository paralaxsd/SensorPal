{
  "ServerConfig": {
    // Set this to your target server's actual IP and port.
    // Users can also override this at runtime via the settings dialog.
    "BaseUrl": "http://localhost:50194"
  }
}